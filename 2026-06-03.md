# 2026-06-03

## Security Audit + 7 Hardening Fixes - DEPLOYED 731f4bd

**Commit:** 731f4bd  
**Branch:** main  
**What changed:** Full security audit followed by 7 hardening fixes applied and deployed.

### Files Modified
- `api/solaredge.ts` - Removed client-supplied API key fallback; server-only env var now
- `api/notify.ts` - Added rate limiting (10/min/user), HTML escaping on all email fields, paginated listUsers()
- `api/send-quote.ts` - Supabase URL from env var
- `api/approve-quote.ts` - Supabase URL from env var
- `api/users.ts` - Supabase URL from env var
- `src/App.tsx` - Removed api_key from SolarEdge proxy URL params (2 call sites)
- `src/components/Settings.tsx` - SMTP password moved from localStorage to sessionStorage
- `src/components/Customers.tsx` - SMTP password read from sessionStorage
- `vercel.json` - Removed `unsafe-inline` from script-src CSP
- `package.json` - xlsx upgraded to 0.20.3 (SheetJS CDN), vite to ~6.3.6
- `pnpm-lock.yaml` - Updated lockfile
- `.gitignore` - Added explicit .env.prod, .env.production, .env.vercel patterns

### Patterns Worth Reusing
- Rate limiting pattern in notify.ts: in-memory Map with per-user token bucket, 60s window
- Supabase URL via `process.env.SUPABASE_URL || 'fallback'` pattern for all API files
- Paginated `listUsers()` loop pattern (page/perPage with break on short batch)
- sessionStorage for sensitive credentials that shouldn't persist across browser sessions

### Security Report
Full Spanish-language report at `SECURITY_REPORT_2026-06-03.md` for client/partner sharing.

### Pending Follow-ups
- Note: pnpm virtual store on local machine has corrupted picomatch symlinks (points to /private/tmp/). Build works on Vercel (Node 20 + clean store). Local fix: `pnpm store prune` + reinstall, or use nvm to switch to Node 20.
- SUPABASE_URL env var should be set in Vercel dashboard for all environments to fully decouple from hardcoded fallback.
