# 2026-06-10

## SolarEdge sync 500 fixed (root /api build chain) + contractor multi-view + Dispatch Map

### SolarEdge 500 root cause + fix (commits `0489ac7`, `73d23c6`, `e865d8d`, `a9918b7`)
The live SolarEdge alert sync returned 500 (`FUNCTION_INVOCATION_FAILED`). Production deploys the **repo-root `/api`** tree. Commit `d0adbaf` (web-push) created a root `package.json` listing only `docx` + `web-push`, which cascaded:

1. **`@vercel/node` missing** , Vercel's function builder now treats root as an npm project and typechecks `/api/*.ts`; every function imports `@vercel/node` but it was undeclared -> all 13 functions failed `TS2307` -> every deploy Errored -> production stuck on a stale broken build serving the 500. Fix: added `@vercel/node` + `@types/node` to root `package.json` (`73d23c6`).
2. **Two masked type errors surfaced** once typecheck ran: `notify.ts` cast `Record<string,unknown>` -> web-push `PushSubscription` (fixed via `as unknown as`), `ups-tracking.ts` used `VercelResponse<T>` (not generic in @vercel/node v5; dropped the type arg). (`e865d8d`)
3. **Hobby 12-function cap** , root `/api` was 13 functions (push-subscribe was the 13th); build completed but "Deploying outputs" Errored. Consolidated the 3 unused Xero endpoints (zero frontend callers) into one action-routed `api/xero.ts` -> 11 functions. (`a9918b7`)
4. Earlier `0489ac7` added `.js` extensions to the **dash** `/api` tree's ESM relative imports (`./_auth` -> `./_auth.js`) , ESM hygiene; dash tree is not the canonical deploy but fixed regardless.

**Verified live:** `/api/solaredge?path=/sites/list` -> 200 (308 sites). `/api/users` -> 401 (was 500), `/api/push-subscribe` -> 405 (exists), `/api/trello-card` -> 400. Production alias = `solarflow-dashboard-sooty.vercel.app`.

PATTERN: a repo-root `package.json` flips Vercel's function builder into typecheck mode; it must declare `@vercel/node`. Build-completes-then-"Deploying outputs"-Error with no build error = the Hobby 12-serverless-function cap. Root `/api` is the canonical tree (native fetch, no `_auth`); dash `/api` is not deployed.

### Contractor T&C: re-prompt fix + reformat (commit `761fb17`, also `8d79b49` catalog)
- **Re-prompt every visit** , gate was only `!contractor.termsAcceptedAt`, which a remote sync can overwrite with a pre-acceptance copy. Added a device-local flag `localStorage['solarops_contractor_terms_<id>'] = TERMS_VERSION` in `ContractorDashboard`; gate is now record OR device. Bumping `TERMS_VERSION` ('v2026.1') re-prompts everyone.
- **Reformat** (`ConexSolTerms.tsx` + `index.css`): removed the top index/TOC, expanded all sections by default (reads as one document), removed the gamified "sections opened" progress bar. Fixed the real formatting bug: body paragraphs used `className="bt"` which was **undefined anywhere** -> styled via scoped `.conexsol-terms .bt` rule.

### Contractor multi-view + Dispatch Map (commit `761fb17`)
Per user decision: contractor portal gets all 4 views; dispatch/ops gets the all-WO map; board read-only; routing is a map+geocoding foundation, agent optimization later via **Google Maps Routes API**.

New reusable `src/components/views/`:
- `jobViewTypes.ts` , `ViewJob` adapter (both `ContractorJob` and staff `Job` map into it) + `BoardColumn`, priority dots, `fullAddress()`.
- `JobBoardView.tsx` , read-only Kanban grouped by status (tap card -> open job).
- `JobCalendarView.tsx` , month calendar of scheduled jobs.
- `JobMapView.tsx` , Leaflet + OSM tiles. Geocodes addresses via existing `addressValidator.validateAddress` (Nominatim, free, no key), caches coords in `localStorage['solarops_geocode_cache']`, colored priority pins (`L.divIcon`, avoids Leaflet's bundler-broken default marker images), `FitBounds`, schedule-order route panel, disabled "Optimize route (coming soon)" stub = Google Routes API integration point.

`ContractorDashboard`: added `view` state + List/Board/Calendar/Map switcher; status filter shows only in List view; maps `ContractorJob` -> `ViewJob` (`documentation` buckets with `in_progress`).

`DispatchMap.tsx` (new staff view): maps `Job` + `Customer` -> `ViewJob` (address from customer), filters to routable/active statuses. Wired: nav item "Dispatch Map" (indented under `jobs`, `MapPinned` icon) in `Layout.tsx`, `case 'routes'` in `App.tsx` (lazy import), manifest entry `routes`.

Deps added: `leaflet`, `react-leaflet@4`, `@types/leaflet`. tsc + vite build green. Dispatch Map render-verified via /snap-ui (routes.png); contractor views build/typecheck-verified (the my-jobs catalog shot shows the T&C overlay because the snap contractor is unaccepted, so the switcher is behind it).

GOTCHA: in `JobMapView`, an arrow `() => [...jobs].sort((a,b)=>cmp, [deps])` swallows the useMemo deps into `.sort()` , use a block body. Job coords are hardcoded `latitude:0/longitude:0` (App.tsx:1404, woHelpers.ts:117), so the map MUST geocode from address; Nominatim is 1 req/sec so pins populate progressively then cache.

## Map view: filter + highlight + multi-select route + assign (commit `808b6e5`)
Enhanced `JobMapView` (reused by contractor + dispatch):
- **Status filter** bar, defaults to **Active** (hides completed/cancelled/closed/paid/invoiced via `isActiveStatus` regex), plus All + a chip per present status. Geocoding now keys off the status-filtered set (`statusFilteredJobs`), not all jobs, so filtered-out orders are never geocoded and selecting never re-triggers geocoding. This also fixed the snap-ui `routes` capture timing out (live geocoding kept the page from reaching networkidle on reload; smaller Active set lets reload complete).
- **Two-way highlight**: hover a list row -> its pin grows + dark border (`highlightId` state); click a pin -> highlight + `scrollIntoView` the row (rowRefs).
- **Multi-select**: row checkboxes + pin-popup "Add to route"; selected pins get an emerald ring. "Selected only" toggle filters map to the selection (contractor uses this to show just its chosen WOs).
- **primaryAction** prop over the selection. `DispatchMap` passes `{label:'Assign to contractor', onClick}` -> contractor-picker modal (approved contractors) -> `App.handleAssignJobsToContractor(ids, contractorId)` sets `contractorId` + `contractorSentAt` per job via `handleUpdateJob` (audited, persisted, auto-mirrors to contractor queue/builds ContractorJob). Contractor map passes no primaryAction so it keeps the disabled "Optimize route" stub.

PATTERN: assign WOs to a contractor = set `job.contractorId` (+ `contractorSentAt`) and route through `handleUpdateJob` (it auto-creates the ContractorJob mirror). Live-geocoding map pages break snap-ui's reload-to-networkidle; geocode only the visible/filtered subset. Render-verified via routes.png (Active filter, status chips, selection checkboxes, Assign button). DEPLOYED 808b6e5 (Ready).

## Contractor photo fixes + Service Order review card (commits `72290f2`, `755d1bf`)
**Photo delete now persists** (`72290f2`): two causes. (1) `App.handleContractorJobUpdate` MERGED contractor photos into admin `woPhotos` (add-only) so a deleted photo survived and reappeared via `toContractorJobView`'s union -> now RECONCILES (drops admin photos not in the contractor's authoritative set; safe because every ContractorJob carries the full set via the view union, so it only shrinks on an explicit delete; `woPhotos` no longer length-gated so the last photo can clear). (2) `photoStore` had no delete so the upload-retry sweep (`listPhotosForJob`) re-added it from IDB -> added `deletePhotoForJobByUrl`; `removePhoto` purges the IDB row. **Audit**: `addPhoto`->`logChange('photo.add')`, `removePhoto`->`logChange('photo.delete')` (category,url,contractor); note/comment edits already audited via field-diff `logJobChange('job.contractor_update')`. **Panel-close hardening**: photo auto-save wraps `onUpdateJob` in try/catch so a mirror throw can't unmount JobDetail (the reproducible "WO window closes" path; `onBack` is never called programmatically + a top-level ErrorBoundary exists, so the close = a render/effect throw). GOTCHA: the category-tab-switch close could NOT be reproduced statically (no save, all `photos[...]` guarded) - if it persists it's likely a mobile-WebView base64 memory crash needing a live repro.

**Service Order review card** (`755d1bf`): new `ServiceOrderCard.tsx` at the top of the contractor WO (JobDetail, all phases). Links WO->SO (shared number, `serviceOrderNo()` SO- prefix), collapsible Scope of Work (from SO line items, description+qty only - no costs per money-hidden rule, description fallback), + Scheduled/Completed/Status/Location, photo gallery (all categories, tap=open), notes. Plumbing: ContractorJob gains `woNumber`+`scopeItems` (new `ScopeItem` type); `toContractorJobView` mirrors `job.woNumber` + maps `job.lineItems`->scopeItems. PATTERN: contractor sees WO- but the SO card cross-references the SO- number (same digits, display-only re-prefix). tsc+build green; SO card is read-only/low-risk, not yet click-through-verified live (behind contractor auth+T&C).

### Pending follow-ups
- Next version: agent route optimization via Google Maps Routes API (https://developers.google.com/maps/documentation/routes) + technician assignment + "add a call to a route". Will likely need a `GOOGLE_MAPS_API_KEY` server proxy (note: root `/api` is at 11/12 functions, room for one more).
- Contractor views not yet click-through-verified live (behind contractor auth + T&C); build/typecheck green.
