
## Grif Blackstone duplicate client: US-15693 merged into US-15708

**Cause.** Blackstone became US-15693 on 08-28 (SolarEdge lead converted, no street address). On 09-22 the Add Customer form made him again as US-15708. The create-time guard `findDuplicateCustomer` keys only on `clientId` and `solarEdgeSiteId`. A new record gets a fresh number before the check and neither record had a site id, so it could never match. Email and phone were identical, differing only in case and formatting (the new phone carried invisible bidi marks).

**Data repair (live, one transaction).**
- Backup: `_bak_blackstone_merge_20260924` (RLS on, 4 rows: both customers, the job, the tombstone list).
- `cust-1790084629995-ns24o7` (US-15708) absorbed the old notes, `howFound`/`referralSource` = SolarEdge Leads, activity union (6 + 7 + 1 merge entry = 14). `solarEdgeClientId` = US-15708, phone stripped of bidi marks.
- SO-2609-99037 (`job-trello-6a8f...`) repointed: `customerId` and `solarEdgeSiteId` -> new id, `fieldTimes` stamped.
- `cust-1787952007748` tombstoned in `deleted_customer_ids` BEFORE the row delete.
- `client_numbers` 15693 kept and renamed "RETIRED: merged into US-15708", so the allocator never reissues a number that already appears on an invoiced order.

**Guard: 5b9fbe1, LIVE** (version.json sha 5b9fbe1, builtAt 2026-09-24T15:16:28Z; "Possible duplicate" string confirmed in live `index-DnvQ4gSr.js`; `/api/users` 401, functions built). `findPossibleDuplicateCustomer` in `lib/dataStore.ts` (email, phone digits, street+zip, name; blank keys never match). Called in `handleCreateCustomer` (App.tsx), the choke point for the Customers modal, lead convert, RMA modal and imports. A soft match asks a human; OK = use existing (callers already release the reserved number). Tests in `customerDuplicateGuard.test.ts`. Build ok, 1040 tests pass.

**Follow-ups.** Registry Google Sheet row for US-15693 still reads Grif Blackstone: mark it retired by hand. 14 legacy pairs sharing a number (09-08 note) are untouched.
