# 2026-06-16

## fd38d1e - Live client confirmation EMAIL on auto-scheduled appointment (Resend, solar.ops@conexsol.us)

User: "lets setup the email. I now have a dedicated email for the app: solar.ops@conexsol.us"
(answer to the client-channel AskUserQuestion). Closes the gap from 3edc91c (was activity/
interaction record only). Builds on the existing Resend setup in api/notify.ts.

- api/notify.ts: NEW branch `if (req.body.action === 'customer-appointment')` right after JWT
  validation - emails the CLIENT directly (no Supabase user lookup, unlike the staff @mention
  path). from 'SolarOps <solar.ops@conexsol.us>' + reply_to same; branded "appointment
  confirmed" HTML template (when / order no / technician). Reuses RESEND_API_KEY (already set
  on Vercel, conexsol.us domain already verified). Returns 502 on send failure, 400 on bad
  email, 200 {sent:0} if no key. KEPT within the Hobby 12-function cap by EXTENDING notify.ts,
  NOT adding an endpoint.
- sendCustomerAppointmentEmail() client helper added next to fireMentionNotifications
  (MentionTextarea.tsx): getSession token -> POST /api/notify {action:'customer-appointment',...}.
- handleContractorProposeSchedule (App.tsx): resolves clientEmail = cjob.customerEmail ||
  crmMatch.email, fires the send non-blocking after the CRM interaction log.
- woHelpers.buildContractorJob: now populates `customerEmail: customer?.email` (was missing ->
  contractor sessions had no email to send to; CRM fallback only works in staff sessions).

VERIFY: Resend send runs on the DEPLOYED Vercel function only (localhost dev server has no
/api), so not testable on the preview. Needs a real schedule action in prod OR a controlled
test POST. tsc -b clean, build clean, 166 tests. PATTERN: customer-facing email = action-branch
on api/notify.ts (avoids new fn under the 12-cap); from solar.ops@conexsol.us; domain already
verified via the notifications@conexsol.us mention emails.

## 3edc91c - Auto-confirm contractor-proposed service date (was: notify office to confirm manually)

User picked "Auto-schedule automation" (AskUserQuestion) for the existing contractor
"Propose service date" feature. Reworked handleContractorProposeSchedule (App.tsx ~1383)
from "ping @cesar @cruz to MANUALLY contact the client" to AUTO-CONFIRM:
- new Job.scheduleConfirmedAt field (types/index.ts, next to contractorScheduleProposedAt)
- auto-advance: if order is pre-dispatch (PRE_DISPATCH_WO set draft/quote_sent/contact_client/
  quote_approved or status 'new'), advance to woStatus 'scheduled'/status 'assigned'
  + stamp contractorSentAt - mirror of the assign auto-dispatch at handleUpdateJob ~1555
- TWO activity notes on the job feed: (1) client confirmation note [outbound, surfaces on
  the customer card Activity tab via the SO-comment mirror], (2) office FYI mention note
  [@cesar @cruz, bell only, "no action needed"]
- office AppNotification reworded title "Job auto-scheduled" / "client notified, no action needed"
- best-effort outbound CRM interaction (loadCustomers/addInteraction/saveInteractions) when
  crmMatch by adminJob.customerId resolves [staff session only; try/catch, contractor session
  has no CRM store -> skipped silently]
- logChange type -> 'job.contractor_schedule_confirmed' { autoConfirmed, advanced }
- JobDetail.tsx contractor UI: heading "Propose service date"->"Schedule service date",
  helper "office notified to confirm"->"date auto-confirmed, client notified, office in loop",
  button "Notify office"/"Office notified" -> "Confirm & schedule"/"Scheduled & client notified"

GAP (told user): NO live customer email/SMS channel exists - api/notify.ts is STAFF-only
(Resend, keyed to staff user IDs + staff auth). So "client notified" = recorded as an
activity note + CRM interaction, NOT an actual outbound email/SMS. A real client channel is
a future task. PATTERN: contractor schedule still gated to real sessions only
(App.tsx ~2542 onProposeSchedule={isImpersonating ? undefined : handler}); verified new UI
labels render via a temp impersonation toggle (reverted, did not click Confirm to avoid
mutating real job data). tsc -b clean, build clean, 166 tests.

## b2912c6 - "No Layout +15%" surcharge added to Optimizer Replacement Pricing Calculator (reverts a37ec52 tabulator approach)

User: "revert the last change. I need the option to be added here" + screenshot of the
Optimizer Replacement Pricing Calculator (ServiceOrderPanel > Parts & Labor tab) showing
the Steep Roof +20% / >3 Stories +15% / Emergency Dispatch +25% checkboxes. The No-Layout
option belongs as a checkbox in THAT calculator, NOT the admin Service Rate Tabulator.

REVERTED the a37ec52 tabulator change (removed noLayoutSurchargePct from types/contractor.ts,
the sr-8 seed in contractorStore, and the RateManagement editable row + handler). KEPT the
On Hold column move from a37ec52.

ADDED to ServiceOrderPanel optimizer calculator: `optCalc.noLayout` bool flag; surcharge
multiplier `if (noLayout) surcharge += 0.15`; "No Layout +15%" checkbox in the Row-2 toggles
array (between Emergency Dispatch and Critter Guard); "No Layout +15%" added to the line-item
flags label in applyOptimizerCalc. Faithful mirror of the existing 4 surcharge flags.
tsc -b clean, build clean, 166 tests. PATTERN: optimizer surcharges live in ServiceOrderPanel
optCalc state (steepRoof/buildingHeight3Plus/specialtyRoof/emergencyDispatch/noLayout), each
a flat +N% multiplier add; NOT in the ServiceRate tabulator.

## a37ec52 - On Hold column moved left of New + Optimizer Change "No Layout" +15% surcharge (tabulator part REVERTED in b2912c6)

User: (1) move the On Hold kanban column to the LEFT of New on the Service Orders board.
(2) Update the Optimizer Change service-type tabulator to include "No Layout (Add 15%)".

(1) Jobs.tsx kanban columns array reordered ['on_hold','new',...] (was ['new','on_hold',...]).
Verified in preview: On Hold renders as the first/leftmost column.

(2) New ServiceRate field `noLayoutSurchargePct?` (types/contractor.ts). Seeded OPT-CHANGE
(sr-8) = 15 in contractorStore.defaultServiceRates. GOTCHA/PATTERN: loadServiceRates()
re-applies the full default by id (keeps only `active`), so the new field auto-propagates
to every existing device on next load - no migration needed. RateManagement "Service Rate
Tabulator": expanded Client Pricing block now has an editable "No Layout +N%" row
(handleUpdateNoLayoutPct persists via onSaveRates, same lightweight pattern as the active
toggle) + computed surcharged client price = clientRateStandard * (1 + pct/100). Surcharge
price shows "-" in UI because SHOW_MONEY=false globally (money lives in Xero); the percent
input + computation are live. tsc -b clean, build clean, 166/166 tests.

NOTE: the Service Rate Tabulator floods the console with the pre-existing data-matrix-id-on-
React.Fragment dev warning (480 entries, dev-tooling only, not in prod build, already flagged
task_ee312c4d). My change added zero new errors.

## a19a896 - US-15478 completion-revert: diagnose + reconcile + drag-to-Completed stamps completedAt

User: US-15478 marked completed but keeps reverting to "assigned." DIAGNOSIS via direct
Supabase query (preview eval): US-15478 = Zoila Posada, WO-2605-97694, job-1779809697696.
SPLIT-BRAIN found: contractor mirror (cj-...m7ym, contractor-4 Jaime) = status completed,
completedAt Jun 14; admin Job (source of truth read by board/pickup) = assigned/scheduled,
NO completedAt, value_updatedAt Jun 13 11:55 (OLDER than the completion). ROOT CAUSE: the
contractor->admin mirror (handleContractorJobUpdate) historically did NOT bump the admin
Job's updatedAt, so when Jaime completed it Jun 14 the admin Job flipped to completed
locally but kept its Jun 13 LWW key -> next pull, server's Jun 13 'assigned' tied/won
remoteWins -> reverted. = the CB-3 defect (already fixed this session, App.tsx ~1255-1261
stamps the admin mirror updatedAt; + d5607e1 handleUpdateJob stamps every admin edit).
Amplified by the cursor-skip bug (this Jun-13 record was one of the stranded ones).

RECONCILED the record: drove the real kanban drop handler (Assigned->Completed) so it
routed through handleUpdateJob (stamps updatedAt + pushes). VERIFIED server-side: status
completed, woStatus completed, completedAt set, fresh updatedAt -> won't revert (no stale
device holds a newer 'assigned').

CODE FIX (a19a896, Jobs.tsx KanbanColumn.handleDrop): dropping a card onto Completed now
stamps `completedAt` (was missing -> a drag-completed WO had no completion date, skewing
the billing lifecycle which keys off completedAt); preserves an existing value. tsc/build
clean, 166 tests.

PATTERN: contractor-completed but admin shows assigned = mirror didn't stamp admin
updatedAt -> completion loses LWW. Any status write that should "stick" must bump
updatedAt. Drag-to-Completed must stamp completedAt for billing.

## 3a92877 - On Hold status filter + dedicated On Hold kanban column (+ Cruz/Cesar hold-count divergence diagnosis)

User: Cruz (Support) sees 69 total / 1 On Hold; Cesar (Admin) sees 63 total / 2 On Hold -
"all users should pull from the same pool." DIAGNOSIS via direct Supabase query (preview
eval, await import('/src/lib/supabase.ts')): SERVER TRUTH = 78 job rows, exactly 1 onHold
(WO-2606-24059 Joaquin Molina), 2 archived. Cruz's 1 hold MATCHES the server; Cesar's 2nd
hold is STALE - a job un-held on the server that his device never pulled (the client-clock
cursor-skip bug, e9f0c3c). Total 63-vs-69 = same cursor skip (Cesar missing ~6 jobs). NOT
a role/RLS difference (Support sees MORE, not fewer). FIX already shipped: server-time
cursor + push-no-advance + SYNC_CURSOR_VERSION full resync (e9f0c3c) + contractor deepSync
(318886d). Cesar's ADMIN device converges on next reload (ensureCursorVersion forces one
full resync on the new build); admins have no manual refresh button, so reload is the path.
(My local preview already reads ~server: 76/1.)

FEATURE (3a92877, Jobs.tsx): (1) "On Hold" added to the status filter dropdown -> lists
only held orders (notHeld override when filterStatus==='on_hold'). (2) Dedicated "On Hold"
kanban column immediately RIGHT OF NEW. Columns array now ['new','on_hold','assigned',
'in_progress','completed','invoiced','paid']; held jobs (orthogonal onHold flag) render
ONLY in that column (status columns filter `!j.onHold`), shown regardless of the Show On
Hold toggle via new boardHeldJobs memo (respects search/contractor/period). KanbanColumn
status type -> JobStatus|'on_hold'; colColors + handleDrop extended: drop on On Hold parks
(onHold=true, keep stage); drop a held card on a status column un-parks (onHold=false) +
sets that stage. VERIFIED in preview: On Hold column right of New holds Joaquin Molina
(WO-2606-24059) with Resume btn; New count excludes it; filter has On Hold. tsc/build clean,
166 tests, no console errors.

## c7e590e - Move SolarEdge Monitoring badge into the SO panel top header row

User request (screenshot of admin ServiceOrderPanel): "add the SolarEdge monitoring Link
badge to the header." The badge already existed BELOW the address; AskUserQuestion ->
user chose "Admin panel top header bar." Moved it into the dark top header row
(ServiceOrderPanel.tsx ~1369) right after `<WOStatusBadge>`, alongside the SO number /
clientId / status. Removed the old copy from the sub-header block (kept the service-type
badge there). Same link `monitoring.solaredge.com/solaredge-web/p/site/<seSiteId>`,
guarded by `seSiteId` (customer.solarEdgeSiteId || job.solarEdgeSiteId). No duplicate.

VERIFIED in preview: opened Joaquin Molina SO -> top row reads "SO-2606-24059 US-15274
Quote Approved [Zap] Monitoring [ext]"; badge gone from below the address; only 1
monitoring link in the DOM. tsc/build clean, 166 tests.

GOTCHA (pre-existing, not from this change): ServiceOrderPanel still floods the console
with the `data-matrix-id`-on-React.Fragment warning (line 397) - already flagged as a
separate task. The contractor side still has NO monitoring badge (ContractorJob lacks
solarEdgeSiteId); user chose top-bar-only, so contractor add was NOT done.
