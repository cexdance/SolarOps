# 2026-06-07

## DEPLOYED 1eff42c..dcc8ca6 (6 commits): Lead Lobby Converted column + nemotron-debug recovery

Pushed `main` (fast-forward from `feat/lead-lobby-converted-column`). Vercel auto-deploys. Production build verified locally: `tsc -b && vite build` exit 0. Tests 127/127.

### Context
A prior debug run by another tool (nemotron) left the working tree non-compiling (Vite babel parse error) and 8 half-finished files. Took over, fixed the blocker, shipped the requested feature, and ran the new SolarOps agent team through a sequenced remediation of nemotron's leftovers. Strict-mode flip (SPEC-F) is intentionally NOT done (gated for explicit go).

### Commits
- **ef74425 feat(lead-lobby): Converted column + App.tsx parse fix.**
  - LeadLobby.tsx: `closed_won` leads no longer vanish. `getLeadCol` now returns `'converted'` (was `null`). New 5th emerald Kanban column "Converted" + a `CONV` stat chip (`convertedCount`). Converted leads kept for growth tracking. Drag INTO Converted = archive (`status: closed_won`); drag OUT reactivates (resets status off closed_won, see `wasConverted` branch in `handleDrop`). Recent vs older (>2 weeks) collapse mirrors the Lost column (`convertedRecent`/`convertedOlder`/`convertedExpanded`). `colLeads` builds converted from a separate `filteredConverted` memo so list view stays active-only.
  - App.tsx: `handleImportApply` had `await validateAddress(...)` INSIDE the synchronous `setData(prev => {...})` updater = Vite/babel "Unexpected reserved word await". Fix: made `handleImportApply` async, hoisted all validation into a pre-pass building a `validatedMap` keyed by item; updater now synchronous and looks up the map. This also brought in nemotron's `addressValidator.ts` feature.
- **070724a chore(cleanup):** deleted dead `auth/LoginScreen.tsx` (zero imports; active login is inline in App.tsx), removed unused `deletePhoto` export, dropped 2 InventoryModule console.logs, eslint no-unused-vars/no-explicit-any off->warn.
- **2512d61 fix(trello):** the real regression nemotron misdiagnosed. `extractContactInfo` changed from `(desc: string)` to `(card: TrelloCardData)` (now mines comments/actions, not just desc). Callers at LeadLobby.tsx:290 and Customers.tsx:5454 still passed `card.desc`. Fixed both to pass `card`. Also 4 TS4111 in trelloImporter via bracket access. `fetchTrelloCard` signature was NEVER the problem.
- **8436de5 feat(sync):** db.ts first-device-init seeding (`{...generateDefaultState(), ...remote}` when localStorage empty), verified merge-safe (pullFromSupabase returns `[]` arrays for empty account so seed customers are correctly replaced; existing local never re-seeded). dataStore now exports `generateDefaultState`. +5 tests (`dbFirstDeviceInit.test.ts`).
- **8705c27 fix(observability):** ~31 empty `.catch(() => {})` / `catch {}` across 14 files now `console.error('[context]', e)`. Swallow semantics preserved (no rethrow, no control-flow change). 7 left intentionally empty (revokeObjectURL, poison-key reset, documented quota fallback, 4 server-only dispatchEvent).
- **dcc8ca6 refactor(types):** `as any` 70 -> 18 (52 removed, type-only, esbuild strips types so runtime bundle unchanged; verified every diff line is a cast/annotation/import/type). Added `SeedCustomer` interface, real type imports. 18 left justified (recharts namespace, `window.google` no @types, 1 undefined-valued field, 13 partial test fixtures).

### Agent team created (`.claude/agents/`, gitignored = machine-local)
master-coder (PM/orchestrator, opus), fatso (DB + DB-to-UI wiring + propagation), cody (rewrites/optimization, opus), karen (UI/UX + Playwright), debugger (pitfall hunting). Master Coder produced the SPEC-A..G roadmap; ran A/B (debugger), C (fatso), D (cody), E (debugger).

### Patterns worth reusing
- await-in-setData-updater is a hard Vite parse error: hoist async work before `setData`, keep the updater pure/sync.
- `as any` removal is RUNTIME-SAFE (type-only) and a good pre-strict-mode step; verify by grepping the diff for any non-type line.
- When a parallel cleanup spans files that may overlap, run worker agents SERIALLY on the shared tree (or use worktree isolation) to avoid edit collisions.

### Pending / follow-ups
- **SPEC-F (gated):** re-enable the 5 strict tsconfig flags ONE at a time to 0 errors each, then commit tsconfig.app.json. The strict-on tsconfig is still dirty in the working tree (uncommitted) as nemotron left it. Pair with finishing the last 18 `as any` and clearing TS6133/TS7030 noise first.
- **SPEC-G:** karen Playwright pass on Trello import (LeadLobby + Customers) and login.
- context-mode MCP was down this session (better-sqlite3 NODE_MODULE_VERSION 141 vs 127); `npm rebuild better-sqlite3` in the plugin cache restores it.
- Untracked at repo root (left alone): generate_security_report.js, package.json, package-lock.json, various .md/.docx/.pdf report artifacts.

## DEPLOYED 44b6a1d..b4f2bb5: SPEC-F strict mode + UI verification pass (SPEC-G)

Continued the roadmap on user "proceed". Two more pushes after the catalog refresh (44b6a1d).

### b4f2bb5 chore(strict): enable strict TS flags, tsc 332 -> 0
- tsconfig.app.json: turned ON noImplicitAny, noUnusedLocals, noUnusedParameters, noUncheckedIndexedAccess, noImplicitReturns, noImplicitThis, noPropertyAccessFromIndexSignature (these were nemotron's uncommitted dirt; now committed). `strict` itself stays false.
- cody drove all 332 errors to 0 across 49 files, type-only/mechanical. Error mix was: 200 TS6133 (unused), 106 TS4111 (dot->bracket on index sigs), 12 TS7030 (missing returns), ~6 implicit-any. noUncheckedIndexedAccess and noImplicitThis added ~0 errors (cheap real type-safety win; the noise was all from the unused + property-access flags).
- Notable: ConexSolTerms styles map switched `as` -> `satisfies Record<string, CSSProperties>` (1 edit cleared 23 TS4111). Positional unused params underscored. Unused useState values dropped while keeping the hook call (preserves hook order).
- **Dead code removed (zero-reference, surfaced by the unused flags):** `handleCleanImport` + `navItems` const (App.tsx), an unwired work-order-create cluster (Customers.tsx: handleCreateWorkOrder/handleServiceSelect/etc.), six dead cell components + TH + handleSort (SolarEdgeMonitoring.tsx). 0 tsc errors proves no dangling callers, so live behavior is unchanged. RECOVERABLE from git (b4f2bb5^) if any was an intended-but-unwired feature, FLAG to user.
- Verify: `tsc -b && vite build` exit 0 WITH strict on; 127/127 tests; net 163 insertions / 751 deletions.

### SPEC-G UI verification (done by me via Claude_Preview, not karen)
Playwright MCP was NOT connected this session, so the karen agent could not run; did the smoke pass myself. Reloaded preview, navigated all 10 top-level views + SolarEdge Sites + Lead Lobby. No Vite error overlay, all screens render. ONLY console error class = the known pre-existing `data-matrix-id` on `<React.Fragment>` in SolarEdgeMonitoring.tsx (tool-injected preview instrumentation, NOT in production). SPEC-F added zero new console errors.

### SPEC-F has zero visual impact -> /snap-ui NOT re-run (catalog from 44b6a1d still accurate).

### New pending
- Confirm with user whether handleCleanImport / Customers WO-create cluster were intended features to restore + wire.
- The whole SPEC-A..G roadmap is now SHIPPED. Remaining 18 `as any` are justified (recharts/window.google/test fixtures).

## DEPLOYED 1d138a5: SolarEdgeMonitoring Fragment console flood fixed
User asked to do the Fragment cleanup now (did it inline, not the chip). The keyed `<React.Fragment key={site.siteId}>` wrapping a main `<tr>` + optional expandable `<tr>` was converted to a returned ARRAY of keyed `<tr>` elements (NOT per-site `<tbody>` - that would break the `divide-y` row dividers; and `div.contents` is invalid inside tbody). data-matrix-id now lands on a `<tr>` (accepts data-*) instead of a Fragment, rows stay direct children of the single tbody so divide-y is preserved. VERIFIED via Claude_Preview: monkeypatched console.error counter (buffer-accumulation gotcha) = 0 data-matrix-id warnings after navigating to SolarEdge Sites, 191 rows render, strict tsc still 0. Zero visual impact -> /snap-ui not re-run. PATTERN: for tool-injected-prop-on-Fragment inside a table, return an array of keyed `<tr>` from .map (div.contents/span/tbody all wrong for table-row siblings; per-tbody specifically breaks the `divide-y` row dividers on the single tbody).

NOTE: the spawn_task chip for this Fragment fix was ALREADY CLICKED by the user (couldn't dismiss), so a separate worktree session got the per-tbody prompt. Told it to discard: issue already resolved on main via the better array approach; per-tbody would regress dividers + conflict with 1d138a5.

## DEPLOYED 961f651 + 8d8c53d: SolarEdge Monitoring em-dash removal
User authorized fixing the pre-existing em dash. Found 5 in SolarEdgeMonitoring.tsx (not just the header), fixed all per the no-em-dash rule (replace with comma/colon, NOT hyphen which is for ranges): header "Florida — Account" -> "Florida, Account"; "Work Orders — {client}" -> "Work Orders: {client}"; "Up to date — N sites" -> "Up to date: N sites"; 2 source comments. Text-only, strict tsc 0. 961f651 = fix, 8d8c53d = /snap-ui catalog refresh (visible text change so catalog re-run, SolarEdge header now "Florida, Account #64793"). Other files still have pre-existing em dashes in UI strings/comments (cody's SPEC-F report noted them) - not yet swept globally.

## DEPLOYED 1c4e1d4 + 17b2cb1: repo-wide em/en dash sweep
User asked for the global sweep. cody agent hit the weekly token limit, so I did it myself with a script (/tmp/emdash_sweep.py). Swept solarflow-dashboard/src + api + scripts (.ts/.tsx/.js/.jsx/.mjs/.css/.html): 669 em dashes + 26 en dashes across 94 files. Rules: spaced " - " separator/aside -> comma; standalone em dash (empty-value placeholders `?? '—'`, signature prefixes) -> plain hyphen; en-dash numeric ranges -> hyphen. KEY INSIGHT: a UNIFORM global string replacement is SAFER than per-occurrence judgment for equality consistency - any literal that is also compared/asserted (UI placeholders, test expectations) gets changed identically on both sides, so nothing breaks. Verified: 0 em/en dashes remain in app source, strict tsc 0, production build exit 0, 127/127 tests, no `, ,` artifacts. 17b2cb1 = /snap-ui catalog refresh (broad UI text change). NOTE: .md docs + JSON were NOT swept (scope = app source); markdown still has em dashes if a future doc-compliance pass is wanted. GOTCHA: the dev server died mid-session again (preview MCP server stops); restart via preview_start before /snap-ui capture. PATTERN: for a huge mechanical text-compliance change, a Python script with uniform replace + tsc/test/build verification beats 600+ manual edits and beats an agent (which can rate-limit).
