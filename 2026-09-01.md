# 2026-09-01

## 223ac66 - CSP blocked the client registry write ("Failed to fetch")

**Symptom:** "Move to Client" on a lead alerted `Could not update the client registry sheet: Failed to fetch. Nothing was converted.`

**Cause:** `claimClientNumber()` (`solarflow-dashboard/src/lib/clientRegistry.ts`) POSTs to the Apps Script `/exec` URL, but `vercel.json`'s `connect-src` never listed `script.google.com`. The browser killed the request before it left, so the `catch` reported a network error and correctly aborted the conversion. Nothing wrong with the script or the sheet.

**Fix:** added `https://script.google.com https://script.googleusercontent.com` to `connect-src` in `vercel.json`. Both hosts are needed: `/exec` 302s to `script.googleusercontent.com`.

**Files:** `vercel.json`

**Verified:** prod `version.json` sha = 223ac66, the live CSP header contains `script.google.com`, `/api/users` still 401s (functions built).

**Pattern worth reusing:** a third-party host added in code needs a matching `connect-src` entry, or it fails only in prod. `vite dev` sends no CSP header, so this class of bug is invisible locally. Same family as the 05-01 `recompressDataUrl()` CSP no-op.

## 2254093 - Click the empty CLIENT NUMBER to claim the next one

**Ask:** in Edit Customer, clicking the client-number field should go to the Google Sheet, take the next available number, assign it, and write the customer name onto that row.

**Implementation:** no backend work needed. `claimClientNumber(name)` with no `clientId` already asks `scripts/client-registry.gs` for the first row whose Name cell is blank (and appends `max+1` when the pre-allocated run is exhausted), and stamps the name. Wired an `onClick` on the input in `Customers.tsx`:
- fires only when the field is EMPTY, so the number stays typeable/editable after it is set
- refuses with an alert when the customer name is blank (the sheet row needs a name)
- sets `clientId` AND `solarEdgeClientId` together (the 08-29 fcfe6f1 rule: storage field and display field are written at the same site)
- placeholder doubles as the state: "Click to claim the next number" / "Claiming..."
- the value only lands in `editForm`; nothing is saved to the customer until Save. The SHEET row is claimed immediately, so a cancelled edit leaves a claimed number behind. Acceptable: the sheet is an append-only ledger and the number can be typed into the next customer.

**Files:** `solarflow-dashboard/src/components/Customers.tsx`

**Verified:** prod sha 2254093, and the string is present in the LAZY chunk `assets/Customers-CJ4HMsbJ.js` (a sha match alone would not prove a code-split change shipped).

**Follow-up:** nothing claims a number for a customer created outside "Move to Client" other than this click. Consider the same handler on the Add Customer modal if the manual path gets used.

## f3a1ba2 - production report emailed as plain text instead of the HTML report

**Symptom:** the client received an email whose whole body was "Please view the attached HTML report for your solar production summary. - Conexsol Service Team". No report, no attachment.

**Cause:** `handleSendReport` in `Customers.tsx` reads the SMTP password from `sessionStorage`. Settings wrote it there too, so it was gone in every new tab. With no password the send fell through to a `mailto:` fallback, and a `mailto:` body is plain text by spec, so the HTML report could never ride in it. The fallback's hardcoded sentence is exactly what the client saw. The SMTP path itself (`api/send-report.py`) was fine all along: it already attaches the full HTML as a `text/html` MIME part.

**Fix:**
- `Settings.tsx` - SMTP password now persists in `localStorage`. Reads fall back to the old `sessionStorage` value once so nobody retypes it; Clear removes both keys.
- `Customers.tsx` - same read order on the send path.
- `Customers.tsx` - deleted the `mailto:` fallback. Missing credentials now set `sendError` ("Email password not set for this session...") instead of opening a compose window that cannot carry the report.

**Files:** `solarflow-dashboard/src/components/Customers.tsx`, `solarflow-dashboard/src/components/Settings.tsx`

**Pattern worth reusing:** a silent fallback to a *less capable transport* is worse than an error. The send "succeeded" every time, so the failure only surfaced when a client mentioned the email looked empty. If path B cannot carry the payload path A carries, it is not a fallback, it is a bug with a friendly face.

**Second pattern:** `sessionStorage` for a credential means the feature silently degrades once per tab. Pick the storage lifetime to match the feature's lifetime, or make the missing-credential case loud.

**Tradeoff accepted by the user:** the SMTP password is now plaintext in `localStorage`, readable by any script on the origin. Upgrade path is recorded in a `ponytail:` comment at `Settings.tsx:136` - move the credential server-side per user.

**Follow-ups:** none blocking. No visual change shipped, so `/snap-ui` was skipped.

## 13b4fbc: Site Transfer card trimmed to the job, flat $120 actual cost

**What changed** (`solarflow-dashboard/src/components/ServiceOrderPanel.tsx`, only file touched):
- `SITE_TRANSFER_COST = 120` module constant. `actualCallCost` short-circuits to it when `isSiteTransfer`, instead of the derived `baseLaborCost + labor + parts + mileage + expenses` sum (which is always ~$0 for a transfer: no contractor, no parts).
- Scope of Work / Notes field hidden for site transfers. The scope is fixed; the identifiers are the work.
- Scheduled Date label renders as "Site Transfer Date" for these orders.
- Labor Hours input hidden (Parts Cost left in place).
- "Submit transfer at SolarEdge" link (https://www.solaredge.com/site-transfer) added inside the identifiers card's button row, beside "Draft email to customer".

**Why:** the card was showing a general service-order shape for a job that is a flat desk task, so the actual cost read as nothing and three inputs never applied.

**Pattern worth reusing:** all five changes gate on the existing `isSiteTransfer` derived flag, no new state, no new component. Hiding via `className={isSiteTransfer ? 'hidden' : ''}` matches the Contractor select right below it.

**Gate:** `pnpm build` green, `pnpm test` 627/627 in 70 files. Live `version.json` = `v1.7.6.2+13b4fbc`, `/api/users` returns 401 (functions built, not NOT_FOUND).

**Follow-ups (not done, user's call):**
- The Parts & Labor tab's own Cost Breakdown total (`totalCost`, ~line 3102) still sums line items, so it can disagree with the $120 header for a transfer.
- `SowDistributionModal.tsx:319` computes its own `actualCost` for the client-facing SOW report; also not $120-aware.

## 591b207: mySolarEdge Site ID wording, customer contact in the SO header

**What changed:**
- `solarflow-dashboard/src/lib/siteTransferEmail.ts`: the Site ID bullet now reads "Your Site ID is in the mySolarEdge app. Tap the menu icon in the top left corner, then tap "Site Details," and your Site ID will be right there." It previously pointed at the attached instructions photo first.
- `solarflow-dashboard/src/components/ServiceOrderPanel.tsx`: customer email + phone render under the site address in the panel header, as `mailto:` / `tel:` links inside a `select-text` container with an explicit `{' '}`-style separator span, so a drag across both copies as "email · phone" rather than concatenated (same trap as cb6352d).

**Why:** Daniel creates the Xero customer straight from the service order header, so the contact details should not be a click away in the client card.

**Gate:** build green, 627/627 tests. Live `v1.7.6.2+591b207`. Verified the lazy `ServiceOrderPanel-DFYaIwPM.js` chunk actually contains both new strings, not just a matching version.json sha.

**Note:** the site-transfer screenshot that prompted this still showed $245.00 and a visible Scope of Work, i.e. a stale client build from before 13b4fbc. A hard reload picks up the $120 flat cost and the trimmed fields.

## b9ea98e - Xero: admin button creates the client and drafts the site-transfer invoice

Live: `v1.7.6.2+b9ea98e.1788285405064`, builtAt 2026-09-01T17:56:45Z. `/api/users` returns
`{"error":"Unauthorized"}` (401 JSON, not a NOT_FOUND page), so the functions built.

### Why
Daniel opened every Xero draft one by one to work out which SO it belonged to, then
hand-created the customer before he could invoice a site transfer. One button now does the
client creation.

### Architecture: the agent is standalone, on purpose
A partner vetoed connecting Xero to SolarOps. Two Supabase edge functions own the Xero OAuth
instead. SolarOps never holds the credentials and has no Xero code path of its own.
Credentials live in Supabase secrets; `xero_auth` has RLS ON with ZERO policies, so only the
service role reads it.

- `supabase/functions/xero-oauth-callback/` - ONE url does both halves of the handshake.
  No `?code` -> 302 to Xero consent. With `?code` -> exchange, store, done. The link Daniel
  clicks IS the registered redirect URI, so the two cannot drift apart.
  **Must deploy with `verify_jwt: false`**: Xero's redirect carries no Authorization header
  and would otherwise 401 with nothing obviously wrong on Xero's side.
- `supabase/functions/xero-site-transfers/` - contact + DRAFT invoice. `verify_jwt: true`.

### Rules worth reusing
- **Xero rotates the refresh token on EVERY use and kills the old one immediately.**
  Persist the new one BEFORE doing anything else: a crash between use and write-back leaves
  the connection dead and needs a manual re-authorisation. Tokens also expire after 60 days
  idle, which is why this runs on a schedule and NOT on Daniel's Mac. A sleeping laptop
  locks itself out.
- **"Has a valid JWT" is NOT authorisation.** The anon key is a valid JWT and ships inside
  the public frontend bundle. `verify_jwt` checks the signature, never WHO. Gate on the role
  claim: `service_role` -> the schedule, `authenticated` -> role must come from `user_roles`,
  anon -> denied. Verified live: anon 403, no token 401, garbage 401.
- **Idempotency from the REMOTE system, not new local state.** Contact looked up by
  `ContactNumber == clientId`, invoice by `Reference == woNumber`. Nothing written back onto
  the Customer record, which syncs whole-record LWW and would let a stale client clobber it.
  Pressing the button twice is safe.
- **A button press that quietly does nothing is worse than an error.** Ineligible jobs return
  a reason: "not a site transfer", "no order number, so this is not a service order",
  "already invoiced".
- **A half-configured integration should do the half it can.** The invoice stays dormant
  until `XERO_SITE_TRANSFER_ACCOUNT_CODE` and `_TAX_TYPE` are set. Guessing a tax code
  misstates the books, which is worse than not automating.

### Live-data findings that changed the design
- **There is no "opens the SO to invoicing" moment.** `getBillingColumn` puts a site transfer
  in `to_invoice` FROM CREATION (Billing.tsx:42-46, deliberate: "no quote and no field work").
  The trigger fires early by necessity.
- **`siteTransferCompletedAt` is unusable as a gate**: set on 3 of 11 site transfers ever and
  on ZERO of the currently-uninvoiced. Gating on it means the agent never fires.
- Of the 2 pending site transfers, **one has no `woNumber`** (US-15017 Parque Solar Doral).
  Since `isServiceOrder(job) = !!woNumber` it is not an order, and the filter excludes it.
- That same customer's `name` is literally `"US-15017 Parque Solar Doral"`, id baked in. The
  id goes in ContactNumber, so `cleanName()` strips it. Test: `cleanName.test.mjs`, 6 cases.
  A name that is ONLY the client id cleans to empty and the record is skipped.

### Files
- `solarflow-dashboard/src/components/ServiceOrderPanel.tsx` - admin-gated button
  (`isAdmin` = admin|coo, line 767), `pushSiteTransferToXero`, supabase import
- `supabase/functions/xero-oauth-callback/index.ts`
- `supabase/functions/xero-site-transfers/index.ts` + `cleanName.test.mjs`

### Follow-ups
1. **BLOCKED on Daniel**: open the auth link once ->
   `https://cjmhfagkkayelcsprbai.supabase.co/functions/v1/xero-oauth-callback`
   Nothing touches Xero until then; the button returns "Not connected to Xero yet."
   That exact URL must also be saved in the Xero app's Redirect URIs box.
2. **BLOCKED on Daniel**: revenue account code + tax treatment for the $120, and the bank
   account contractor Zelles post against.
3. **The button label overpromises** until (2) lands: it says "Create client + draft invoice"
   but reports `Invoice: skipped`. Soften to "Create client in Xero" if (2) drags.
4. **No cron yet, on purpose.** Do not schedule an unproven writer. Schedule after watching
   one real run.
5. **No visibility at all**: nothing records whether a run happened or failed. A dead token
   would surface only as Daniel noticing drafts stopped. Add a run log + failure email.
6. **Partner note**: SolarOps can now TRIGGER a Xero write. Credentials still never touch the
   app, but that is a different shape than "a separate agent on a timer". Tell them.
7. Phase 1 (contractor bills: write the SO into the bill Reference, stamp `costsCoveredAt`
   when a bill flips to PAID) is designed in `CONTRACTOR_PAYMENT_PLAN.md`, not built.

## e2e1d6b - the Xero button was in the CLIENT-FACING SOW report, not the panel

Live: `v1.7.6.2+e2e1d6b.1788287012577`. New label present in `ServiceOrderPanel-C0wOkr74.js`,
old label gone.

### The bug
The user could not find the button. It WAS in the prod bundle (verified by grepping the live
lazy chunk), so it was a render-location bug, not a deploy one.

I anchored the insert on the note "Admin team runs the SolarEdge ownership transfer via
agentic workflow...". That string lives inside the **SOW Report Modal**
(`{showSowReport && (() => {`, `id="report-print-area"`), not in the panel body. So the
button only existed on the printable scope of work, which is CLIENT-FACING.

**RULE: a unique anchor string is not a unique LOCATION.** Before inserting JSX, walk the
ancestor chain by indentation and confirm the enclosing gate. The chain here was
`showSowReport -> return( -> modal div -> report-print-area`, which would have been obvious
in one check. There are TWO site-transfer cards in this file: the editable one in the
Overview tab (line 2338) and a rendering inside the SOW report (line ~3955 pre-fix).

Correct chain now: `activeTab === 'overview'` -> `isSiteTransfer` -> `isAdmin`, in the
Site Transfer Identifiers card beside "Submit transfer at SolarEdge".

### Also in this commit: the Xero app is refused accounting.transactions
Tested every scope against the authorize endpoint, which needs NO credentials and NO consent:
an unusable scope 302s to `/identity/error` with `invalid_scope`.

```
NO   accounting.transactions, accounting.reports.read, accounting.journals.read
yes  accounting.contacts, accounting.attachments, accounting.settings,
     budgets.read, files, assets, projects, payroll.employees
```

Not a read/write split, and NOT the redirect URI (that is correct and accepted). Scopes
trimmed to what the app allows so Daniel can connect today; label narrowed to "Create client
in Xero" because the invoice would always report "skipped". `XERO_SCOPES` env var restores
transactions without a redeploy; re-authorise with `?force=1`.

**RULE: test an OAuth app's scopes against the authorize endpoint BEFORE building on them.**
Second time this shape has bitten: the Trello token was read-only for the same class of reason.

### Settled
- The $120 site transfer is a FLAT FEE with NO TAX (user). US Xero org -> `TaxType: "NONE"`.
- Still needed from Daniel: revenue account code, the Zelle bank account, and the
  accounting.transactions permission on the app.

### Consequence for scope
Phase 1 (contractor bills: write the SO into the bill Reference, mark paid, stamp
`costsCoveredAt`) is blocked ENTIRELY on accounting.transactions, not just the invoice half.

## e2e1d6b - Excel import: every row becomes a PowerCare customer + service order

Shipped inside commit `e2e1d6b`, whose message describes only the Xero button
move. A second session committed concurrently and swept these staged files into
its commit; the code is correct and tested, but the commit message does not
describe this half of it. This section is the record.

### What was wrong

The "Import from Excel" affordance in Add Customer read **row 1 only**, mapped 8
contact fields onto the form, and dropped every other column. The sheet
SolarEdge sends ("Cases for Conexsol 8.31.26.xlsx") is the PowerCare work queue:
each row is a customer AND the order to run it, and the discarded columns (part
numbers old + shipped, serials, tracking, ship dates, work description, case
subject, account) are the reason the sheet gets sent at all.

### What it does now

- `readSheetRows` reads every row, not just the first.
- `rowToNotes` folds each column with **no form field of its own** into the
  notes. Header-driven, so a new column in a future export lands in the notes
  automatically rather than being silently dropped.
- `rowToPowerCareRecords(row, woNumber)` builds the customer + service order
  pair: `isPowerCare` on the customer, `isPowercare` on the job,
  `woStatus: 'contact_client'` (the PowerCare entry stage: SolarEdge pays, so
  there is no quote to send or approve).
- The case number leads the order notes in the `Case #: N` shape
  `findPowercareCaseNo` already scans for, so it reaches the SO header **with no
  new field and no synthetic RMA record**.
- `ImportPrefill` gains `onImportAll`. Only a host that can create both a
  customer and an order supplies it (Customers.tsx). CustomerManagement.tsx has
  no job creator and keeps the old single-row prefill, unchanged.

### Rules worth reusing

- **An id minted from `Date.now()` collides the moment anything creates records
  in a loop.** Three separate mints were broken and each ate rows silently:
  - `generateServiceOrderNumber()` was `Date.now()` last-5-digits. N rows in one
    millisecond got the SAME SO number, and `handleCreateJob` dedupes by
    `woNumber`, so **two imported rows would have collapsed into one order**.
    Now monotonic per session.
  - `cust-${Date.now()}` / `job-${Date.now()}` collided the same way. Random
    suffix added; verified nothing parses these ids first.
  - This is the same family as 08-24 0285c60 ("an id minted at the SAVE SITE is
    minted once per save, not once per record"). **Before writing any bulk loop,
    grep the mint sites it will drive.**
- **`cellDates: true` is not enough for Excel dates.** It only converts cells
  XLSX *types* as dates, and this export number-formats them, so ship dates
  arrived as the serial `46261.55`. Converted **by header name** (`/date|eta|
  delivery/`), because guessing from the value alone would mangle a case number
  (7162665) or a latitude (25.94). Agrees with `XLSX.SSF.parse_date_code`.
  Excel itself displays these cells as the bare number, so the app now reads
  better than the source.
- **A `toEqual` test on a mapper's whole output is a tripwire for "and now it
  carries more".** `leadImport.test.ts` failed the moment notes started
  capturing unmapped columns. That was the test doing its job; updated to assert
  the new contract rather than loosened.
- **Test fixtures invented from memory lie.** Two of my assertions (`Florida` vs
  `FL`, ship date `08-28` vs `08-27`) were wrong, not the code. The real file
  and `XLSX.SSF.parse_date_code` settled both. **Read the actual artifact.**

### Deliberate omissions

- **No `clientId` is claimed.** `US-XXXXX` comes from the Google Sheet
  (08-28 e951907, 09-01 2254093), not a local counter, and the empty
  client-number field already claims the next one on click. Minting locally
  would diverge from the sheet.
- **No `RMAEntry` is created**, though the rows carry shipped part + tracking.
  Inventing one would put a row on the RMA dashboard nobody filed. Add when the
  RMA modal can claim it.
- Within a batch, a repeated customer (one client, two open cases) is created
  once and both orders hang off it. `onCreate` cannot see records made earlier in
  the same tick (it reads the previous render's list), so the loop keeps its own
  index.

### Files

`solarflow-dashboard/src/lib/leadImport.ts` (readSheetRows, rowToNotes,
rowToPowerCareRecords, cellText/cellDisplay), `lib/woHelpers.ts` (monotonic SO
number), `components/ImportPrefill.tsx` (onImportAll), `components/Customers.tsx`
(importPowerCareRows), `App.tsx` (uniqueSuffix on record ids),
`__tests__/powerCareImport.test.ts` (new, 8 cases), `__tests__/leadImport.test.ts`.

### Verified

`pnpm build` + `pnpm test` green (71 files, 635 tests). End-to-end against the
real 2-row file: 2 customers, 2 **distinct** SO numbers, both case numbers
resolving on the SO header, 19 notes lines each. Live: `version.json` sha
`e2e1d6b`, `/api/users` returns a real JSON 401 (functions built).

### Follow-ups

- **Concurrency hazard, not code:** two Claude sessions were writing this repo at
  once. The other session's `git commit` captured my staged index. Nothing was
  lost, but the commit message is now wrong for half its content. **When another
  session may be active, commit immediately after staging, or work on a branch.**
- The one-line `RMADashboard.tsx` `Case #{caseNumber}` fallback is **still
  uncommitted** (unchanged from before this session). Ship-or-drop.
- `woStatus: 'contact_client'` for imported orders is my call, not the user's.
  Change to `draft` if the board wants them staged earlier.

## a9affcb - re-importing a PowerCare sheet enriches, it does not clone

### The live finding that changed the design

Asked to "re import last 2 xls", checked live Supabase before touching anything.
Both customers were **already on file**, created 17:57 and 17:58, about 30 min
before e2e1d6b went live, by the old row-1 prefill used twice by hand:

| | Carlos Diaz | Wilber Vega |
|---|---|---|
| record | `cust-1788285446169` | `cust-1788285518430` |
| clientId | US-15689 | US-15690 |
| isPowerCare / case # / tracking | none | none |
| notes | empty | empty |
| service order | none | none |

So the records carry the one thing the importer cannot produce (a claimed
`US-XXXXX` from the Google Sheet) and nothing else. A straight re-import would
have produced **4 customers**: the import claims no `clientId`, and
`findDuplicateCustomer` keys only on `clientId`/`solarEdgeSiteId`, so it is
blind to them.

**RULE: before re-running an import, query live for what the last run left
behind.** "Re-import" reads like a repeat of a clean action; it is usually a
merge into partial state. The blast radius here was 2 duplicate clients with
split client numbers, which is the kind of thing nobody notices for weeks.

### What shipped

- `matchExistingCustomer` - email, then phone, then name, all normalized. **A
  blank key matches NOTHING**, or every record with no email collapses into one
  client. Same trap `findDuplicateCustomer` documents in its own comment.
- `enrichCustomerFromRow` - adds ONLY the PowerCare fields and the sheet block.
  Never touches name, address or clientId: **the record on file was reviewed by
  a human and the sheet was not**. Appends the block only when it is not already
  present, so a third run is a no-op.
- `findOrderForCase` - **idempotency comes from SolarEdge's case number**, not
  from anything this app mints (same rule as the Xero work: take the id from the
  remote system). A case that already has an order does not get a second one.
  Bounded with `(^|\D)N(\D|$)` so 7162665 does not match 71626650, and requires
  `woNumber` so a lead never counts as an order.

The summary line now reports all four outcomes separately (created, enriched,
orders created, cases skipped) rather than one count, because with enrichment in
play "imported 2" would hide whether anything was actually linked.

### Files

`lib/leadImport.ts` (3 new exported helpers), `components/Customers.tsx`
(`importPowerCareRows` rewritten; modal now takes `existingCustomers`,
`existingJobs`, `onUpdateCustomer`), `__tests__/powerCareImport.test.ts`
(12 more cases, 20 total).

### Verified

`pnpm build` + `pnpm test` green (647 tests). Live `version.json` sha `a9affcb`.
**Not yet exercised against the real sheet** - the next import of
"Cases for Conexsol 8.31.26.xlsx" should report *2 existing customers updated,
2 service orders created*, and a second run of the same file should report
*2 cases already had an order (skipped)*.

### Follow-ups

- The two enriched customers keep US-15689 / US-15690. The importer still claims
  no client number for genuinely new rows, by design.
- `enrichCustomerFromRow` keeps an existing case number over the sheet's. If one
  client legitimately has a second case, the customer-level field holds the
  first; the per-order number is in the order notes and is what the SO header
  reads, so the header stays correct. Only the customer card can go stale.

## 456f791 + edge fn v4 - two masked failures on the Xero button

**1. "Could not reach Xero" was my own error handling.** `supabase-js functions.invoke()`
returns `data: null` on a non-2xx and hangs the real Response off `error.context`. The
handler looked for the reason in `data`, never found it, and fell through to the generic
catch. Every useful message was swallowed: "Not connected to Xero yet", "admin only",
"already invoiced", "not a site transfer". **RULE: a generic failure message is a bug in its
own right - it converts a precise diagnosis into a guess.**

**2. "Failed to send a request to the edge function" was CORS.** The browser preflight asks
for `x-client-info` (supabase-js ALWAYS sends it) and the function's
`access-control-allow-headers` listed only `authorization, content-type, apikey`. A header
missing from that list makes the browser refuse to send the POST at all, so the request never
leaves. **RULE: echo `access-control-request-headers` rather than hardcoding a list.**
Verified all four headers now allowed.

**Xero is CONNECTED**: org `Conexsol`, 2026-09-01 18:31:17Z.

Also in edge fn v4: invoice creation wrapped in its own try/catch so an invoice failure no
longer discards the contact result, and `TAX_TYPE` defaults to `NONE` (flat fee, no tax).
The `_TAX_TYPE not set` guard was dropped since the tax answer is settled; only
`ACCOUNT_CODE` still gates the invoice.

## 40927ae - Move to Client refuses to file a lead under a stranger

Deployed and verified live: `sha: 40927ae`, `builtAt 2026-09-01T21:16:44Z`, guard string confirmed
inside the LAZY chunk `assets/Jobs-wOABRjdN.js`, `/api/users` returns 401 JSON (functions built).
657/657 tests pass.

### The incident
Converting the Danielle Ferrari lead created NO customer. It filed her under **Andres Jimenez**
and merged her two call logs into his activity history.

Chain, from `change_log`:
1. 16:19:48 - Cesar hand-edits Jimenez's client number `US-15683` -> `US-15688`. The registry
   sheet is never told.
2. 20:59:14 - "Move to Client" on Ferrari. `claimClientNumber` asks the sheet for the next free
   row and gets **US-15688** (the sheet still had it blank).
3. `findDuplicateCustomer` keys on `clientId`, matches Jimenez. Logged
   `customer.create.duplicate_blocked`, `absorbed:true`. Returns HIS id.
4. 20:59:23 - `handleConvertLead` stamps the lead with `customerId: cust-1787745932670`.

### Root cause
`handleCreateCustomer` returns an id with **no signal of whether it created or matched**, and
`handleConvertLead` used it blindly. Deeper: a number the sheet just reported as FREE is then
used as the dedupe key. A match there is always sheet/CRM drift, never a real duplicate.

### Fix
`clientNumberOwner()` in `lib/leadConvert.ts` (pure, excuses the lead's own customer so
re-converting still works) + a refusal in `Jobs.tsx:775` naming the client holding the number.
`__tests__/leadClientNumberCollision.test.ts`, 5 cases.
Rode along in the same file: the previously uncommitted "Clear filters" button.

### RULES
- **An id returned by a create path that also dedupes is not proof anything was created.** If the
  caller wires other records to that id, it needs the created/matched distinction, not just the id.
- **Never dedupe on a key you minted one line earlier from an external allocator.** The allocator
  said free; the local match therefore means the two systems disagree. Stop, do not merge.
- **A "duplicate blocked" alert is not a safe no-op** when the caller ignores the return value and
  keeps going. The user saw the alert and the lead was still misfiled.

### Registry reconciliation (live read of the sheet, 767 rows)
`reports/powercare-registry-sync-2026-09-01.md`. Sheet convention is
`<Name> POWERCARE: <case#>` in the Name column (79 rows use it).
- 58 of 75 PowerCare customers already correct; **13 need the case number added** (exact rows in
  the report); 2 have no case number anywhere; 1 disagreement (Ariel Alfonzo US-15433, sheet
  6055345 vs CRM 6477709).
- **US-15689 / US-15690 are blank in the sheet but already given to CARLOS DIAZ and Wilber Vega
  in the CRM.** The next two conversions would have repeated the Ferrari failure exactly. The new
  guard now blocks them with a message; the sheet is still wrong.
- **The Apps Script `/exec` cannot do the 13 updates**: it takes only `{name, clientId}` and
  refuses a row that already holds a different name (`taken:true`). Hand-edit, or add an
  overwrite flag to `client-registry.gs` and redeploy a NEW version.

### GOTCHA
**A locally built chunk hash does NOT match prod's.** `dist/assets/Jobs-6kBfztoC.js` locally,
`Jobs-wOABRjdN.js` live; fetching the local name 404s. Crawl the real graph from `index.html`
(110 chunks) and grep there.

### Still open
- Danielle Ferrari has no customer record and no number. First number free in BOTH systems is
  **US-15691** (15689/15690 are taken in the CRM). Her two call logs are still on Jimenez.
- CRM/sheet off-by-one below 15685: 15682 sheet=Carlos Bernal / CRM=Travis Fullenkamp;
  15683 sheet=LG ESS / CRM=nobody; **15684 holds TWO CRM customers**, Carlos Bernal and
  Edward Olivares.
- Linda Mclaughlin sits on US-15613 (CRM) and US-15627 (sheet, with case 6777161).
- `/snap-ui` owed: the "Clear filters" button is a visible Jobs-board change.

## Data repair: Ferrari created, Jimenez cleaned, registry rows claimed

Live writes, verified by read-back.

**Google Sheet** (via the existing Apps Script `/exec`, blank rows only):
- US-15689 row 676 -> `CARLOS DIAZ POWERCARE`
- US-15690 row 677 -> `Wilber Vega POWERCARE`
- US-15691 row 678 -> `Danielle Ferrari`
Next claim now lands on US-15692, free in both systems. The 15689/15690 trap is closed.

**Supabase**
- Created `customer:cust-1788298000000-fer91`, Danielle Ferrari, US-15691, site 451846,
  dferrari@valhallaresale.com / 954-871-5988, zip 33604, with the lead notes and both call logs.
- `job:job-trello-6a91d251fe40205f1bf66989` repointed: customerId + clientId + solarEdgeClientId,
  `fieldTimes` bumped on the three changed fields so field-level LWW keeps the repair.
- `customer:cust-1787745932670` (Jimenez): removed `lead-log-1787949361644`,
  `lead-log-1787956026735` and `activity-1788296363797`. 12 -> 9 entries, 0 lead-logs left.
  His US-15688 was left alone, it is what the sheet says.

**GOTCHA: `app_data.value` is `jsonb`, not text.** `set value = (...)::text` fails with 42804.
The `value::jsonb` casts scattered through earlier queries were no-ops.

**⚠ A stale browser tab is still a live sync client.** Any tab open since before 21:25 holds the
old Jimenez record and can re-push those 3 activity entries under whole-record LWW. Hard-refresh
every open tab.

**PowerCare backfill, the 13 rows: still owed.** `/exec` refuses a row that already holds a
different name, confirmed live against US-15459 (`taken:true`, sheet unchanged). Wrote
`scripts/backfill-powercare-cases.gs`: a one-off with a `dryRunPowerCareBackfill` and a
`backfillPowerCareCases`, run from the Apps Script editor. **Running a function does not need a
new deployment**, so `/exec` is untouched. It verifies the current cell text before writing and
reports any row someone else has edited instead of overwriting it.

## Correction: rows 676/677 were claimed without their case numbers

I wrote `CARLOS DIAZ POWERCARE` and `Wilber Vega POWERCARE` because the CRM held no case
number for either. The numbers were never missing, they were in the SOURCE file and had
simply never reached the CRM: `~/Downloads/Cases for Conexsol 8.31.26.xlsx`, column
`Case Number`.

- CARLOS DIAZ, 5604 Northwest 189th Terrace, Miami Gardens 33055 -> **7162665**
- Wilber Vega, 8014 Thames Place, Tampa 33610 -> **7099775**

Matched on address + email + zip against the CRM records, not on name alone.

**RULE: "the CRM has no case number" is a statement about the CRM, not about the world.**
Both customers were hand-entered at 17:57 on 09-01 with empty notes and no job, so every
column of the source sheet was dropped. Before writing a placeholder, go back to the artifact
the record came from. Same family as the 09-01 "a fixture invented from memory lies" rule.

Wrote `powerCareCaseNumber` + `isPowerCare` onto both CRM customers.
Rows 676/677 added to `scripts/backfill-powercare-cases.gs`, now 15 rows: `/exec` will not
touch them any more either, since they now hold a name.

**The rest of the PowerCare data for these two is still missing** (tracking number, ship date,
ETA, POD, part/serial numbers). That is exactly what the a9affcb re-import enrichment path is
for: re-import `Cases for Conexsol 8.31.26.xlsx` through the app and it fills those fields on
the existing records instead of cloning them.

## Re-imported Diaz + Vega from the 8.31.26 sheet, server side

Ran the app's OWN import helpers against `~/Downloads/Cases for Conexsol 8.31.26.xlsx` via
`npx vite-node`, rather than hand-formatting the block: `mapRowToContact` ->
`sheetImportActivity` -> `enrichCustomerFromRow` from `lib/leadImport.ts`. The comment text is
therefore byte-identical to what the in-app import would have written.

**RULE: to reproduce an import outside the UI, RUN the importer, do not re-implement it.**
`vite-node` loads the real TS module with its real `xlsx` dependency, so the Excel date serials
(46261.28 -> 2026-08-27), the leading-apostrophe phone unforcing and the `rowToNotes` skip-set
all behave exactly as in the app. A hand-written block would have drifted on the first one.

Applied to both customers: `isPowerCare`, `category: PowerCare`, `powerCareCaseNumber`,
`powerCareTrackingNumber`, `powerCareShipDate`, `powerCareETA`, `powercarePOD`, the notes block,
and the `xls-case-<n>` comment prepended to `activityHistory`.

- **US-15689 CARLOS DIAZ** case 7162665, UPS 1Z769A050307710063, shipped 08-27, POD 08-29,
  SE7600H-US000BNU4 -> SE7600H-USMNUBL75. 2 comments (his earlier one kept).
- **US-15690 Wilber Vega** case 7099775, UPS 1Z769A050307707031, shipped 08-27, POD 08-28,
  SE10000H-US000BEU4 -> SE10000H-USMNUBL75. 1 comment.

The comment id is `xls-case-<caseNumber>`, derived not minted, so an in-app re-import of the
same sheet recognizes this entry and does NOT stack a second copy.

Beyond the importer: also set the four typed shipping fields (`powerCareShipDate`/`ETA`/`POD`)
that `enrichCustomerFromRow` leaves alone. It only writes case + tracking; the dates live in the
notes block. **Candidate follow-up: teach `enrichCustomerFromRow` those three fields**, since the
UI has them and the sheet always carries them.

## Sheet check: column A is not the row number

Reported as "675 US-15689 US-15689 / 676 US-15690 Wilber Vega POWERCARE". Those leading numbers
are **column A**, the running counter, which sits ONE BEHIND the spreadsheet row: US-15689 is
column A 675 and sheet row 676. Reading a position off column A and editing there shifts every
edit up one row.

**RULE: a sheet with its own index column has two row numbers, and only one of them addresses a
cell.** Re-verified all 15 backfill targets against a live read: 14 exact, so the script's row
numbers are right.

**Row 676's Name cell was overwritten by hand with the literal text `US-15689`.** It read
`CARLOS DIAZ POWERCARE` when I wrote it and when I read it back. No app path can produce that
value: `handleClaimClientId` (Customers.tsx:3453) refuses an empty name and never passes the
client id as the name, and `handleConvertLead` sends `payload.name`. Script now accepts either
value for that row so the backfill still repairs it.

**Neither row has its case number yet, and would not.** `/exec` refuses a row holding a name,
so 15689/15690 were always part of the `.gs` backfill, which has not been run.

## PowerCare backfill applied: all 15 rows

Could not run the `.gs` remotely (no Apps Script execution from here). Drove the signed-in Chrome
instead and edited column C directly, addressing each cell through the Name Box rather than
clicking the grid, since Sheets renders cells to canvas and they are not in the DOM.

Verified on the server afterwards with an independent gviz read: 15/15 exact.

| Row | Number | Name now |
|-----|--------|----------|
| 446 | US-15459 | Michael Fitzi POWERCARE: 6083080 |
| 461 | US-15474 | George Rohling POWERCARE: 6020784 |
| 464 | US-15477 | Liuba Medina POWERCARE: 6200992 |
| 495 | US-15508 | Dayami Pantoja POWERCARE: 6226749 |
| 506 | US-15519 | Sylvia Fox POWERCARE: 6083050 |
| 516 | US-15529 | Osama Abuadieh POWERCARE: 6429730 |
| 537 | US-15550 | Lee Duerr POWERCARE: 6517658 |
| 566 | US-15579 | Liliane Grand'Bois POWERCARE: 6537090 |
| 616 | US-15629 | Elijah Hopkins POWERCARE: 6871714 |
| 617 | US-15630 | Pete Zittere POWERCARE: 6887843 |
| 629 | US-15642 | Ernesto Velazquez POWERCARE: 6866663 |
| 653 | US-15666 | Charles Bingham POWERCARE: 7021729 |
| 654 | US-15667 | Todd Farley POWERCARE: 6995588 |
| 676 | US-15689 | CARLOS DIAZ POWERCARE: 7162665 |
| 677 | US-15690 | Wilber Vega POWERCARE: 7099775 |

**Row 566 lost its trailing "- [Enhanced Service] S440 (11) Optimizer RMA"**, by design: the
convention is `<Name> POWERCARE: <case#>`. Re-add the RMA text in the DESCRIPTION column if it
still matters.

**RULE: before writing to a row addressed by number, confirm the number against the GRID, not a
CSV export.** gviz can collapse rows, and column A here is a counter that runs one behind the row
in the tail and two behind at the top (it is not perfectly sequential). Three visual spot checks
(670 = LG ESS, 653/654 = Bingham/Farley, 676/677) proved the mapping before any write.

`scripts/backfill-powercare-cases.gs` is now redundant but kept: it is the repeatable path for the
next sheet, and it verifies each cell before writing.

### Still open
- **US-15433 Ariel Alfonzo (row 420)**: sheet case 6055345 vs CRM 6477709. Unresolved.
- **Linda Mclaughlin on two numbers**: US-15613 row 600 (CRM, no case) and US-15627 row 614
  ("Client: Linda Mclaughlin POWERCARE: 6777161"). One row should be freed.
- **US-15341 Steven Mellion**: PowerCare with no case number anywhere.

## a268edc - the keystroke-comment glitch

Live `sha: a268edc`, `/api/users` 401 JSON. 662/662 tests pass.

### Symptom
A work order's comment thread was 116 entries reading "B", "Ba", "I", "In", "Inv"... one
comment per keystroke, spelling out a single sentence (SO-2608-07213, Hannah Gunnoe).

### Root cause
`App.tsx handleUpdateContractorJob` mirrors the contractor's service note onto the job's
activity feed. The entry's id was a **hash of the note text**:

    let h = 0; for (...) h = (h*31 + contractorNote.charCodeAt(i)) | 0;
    const noteId = `cnote-${updatedJob.sourceJobId}-${h >>> 0}`;
    if (!woActivityHistory.some(a => a.id === noteId)) { ...append... }

The comment said the id was "content-stable" so a re-synced note would collapse to one entry.
It does do that. But the field app saves on EVERY KEYSTROKE, so each prefix is a different
string, hashes differently, and appends its own entry.

**RULE: a content-derived id cannot dedupe a value that changes on every save.** The dedupe key
has to be stable over exactly the thing that varies. Here the note text was the variable, so
hashing it guaranteed a miss. Identity belongs to the SLOT (this job's field note), not to the
value in it.

### Blast radius, measured before fixing
1,690 mirrored comments across 28 service orders, 2026-06-30 to 2026-08-25. Worst offenders
SO-2607-23654 (199), SO-2608-87946 (177), SO-2607-92816 (161).

### Fix
`mirrorContractorNote()` in `woHelpers.ts`: stable id `cnote-<sourceJobId>`, replace in place.
`unionById` keeps the winner's copy, so two devices collapse to one entry rather than both.
5 tests, including the mid-text-edit case.

**A prefix check would NOT have been enough.** SO-2608-82375's 112 entries run 176 -> 175 -> 174
-> 175 chars: the contractor was editing INSIDE the text, so no entry is a prefix of another.
Cleanup therefore clustered by time (gap >= 10 min starts a new run), kept the last of each run,
then dropped any survivor that was a strict prefix of another. 1,690 -> 28, one per job.
Backup: table `_bak_cnote_20260901` (28 rows, RLS enabled).

Spot-checked the one 2-char survivor: SO-2607-54917 keeps "in" and its `serviceReport` is empty,
so the contractor really did type and abandon it. Real data, not a cleanup artifact.

### ⚠ The cleanup can partially resurrect
`activityHistory` merges with `unionById`, which only ever ADDS. A device still holding the old
1,690-entry arrays in IndexedDB will re-contribute them on its next sync. The code fix stops new
growth permanently; the delete holds only once every device has re-pulled. Re-running the cleanup
query is cheap if a thread reappears.

### Follow-up worth taking
The field app saving on every keystroke is the upstream cause and is still there. It also means
a full job push per character. Debouncing the contractor note field would cut sync traffic
sharply. Not done here: the fix above makes it harmless to the feed.

## 939cf33 - debounced the contractor job editor's auto-save

Live `sha: 939cf33`, `/api/users` 401 JSON, flush hooks confirmed in the shipped chunk
`assets/index-Cb-gSgIG.js`. 669/669 tests pass.

The upstream half of the keystroke-comment glitch. `JobDetail`'s auto-save effect depends on
three FREE-TEXT fields (`serviceNotes`, `nextSteps`, `upsellNotes`) and wrote the whole job on
every change, fanning it out through the sync engine: one full job push per character typed.

Now trailing-debounced at 700ms.

**RULE: a debounce on a field holding unsaved user input is a DATA-LOSS path, not an
optimisation.** Every way the editor can go away has to flush: unmount, `visibilitychange` to
hidden, and `pagehide`. The last two are not optional here, this is a phone in the field where
switching apps or locking the screen is the normal way a form gets left. Same family as the
08-24 f3c7641 rule about a component holding user input not depending on staying mounted.

**RULE: rebuild the payload at SAVE time, not at SCHEDULE time.** The snapshot comes from a ref,
so the write that lands carries the latest state instead of whatever was on screen 700ms ago.

Pending/flush semantics live in `lib/debouncedSaver.ts` so they are testable without React (this
repo has no component-effect test setup, only `renderToStaticMarkup`). 7 tests: burst collapsing,
the change-then-immediate-flush tail, flush as a no-op when idle, and a throw inside save not
leaving the timer armed to repeat the write.

**GOTCHA: adding a cleanup return to an existing effect can break the build.** The early
`if (!isMounted.current) { ...; return; }` became TS7030 "not all code paths return a value" the
moment the other branch started returning a cleanup. `return undefined;` on the guard clause.
Caught by `npx tsc -b`, which `npm run build` also surfaces.

## ff75077 - UI catalog refreshed (/snap-ui)

25/25 screens re-captured at 1440x900, `UI_SCREEN_CATALOG.md` rebuilt. This was the `/snap-ui`
owed since 40927ae added the "Clear filters" button to the Service Orders board. All 21
authenticated PNGs changed (they render live data); the 4 pre-login shots are unchanged.

**GOTCHA: port 5173 was already taken by another dev server**, so `vite` silently fell through to
5174 and the default `--url=http://localhost:5173` would have captured a DIFFERENT process's
build. Both ports served the same app, so nothing would have looked wrong. Passed
`--url=http://localhost:5174` to point at the server started from the current tree.
**RULE: when a tool defaults to a port, confirm the process on that port is YOURS.**

Spot-checked two PNGs: `admin/jobs` (Service Orders board, 110 showing, footer v1.7.6.2 db 09-01)
and `contractor/my-jobs`. Both authenticated with real data, not the login page.

Note: `contractor/my-jobs` is captured with the contractor terms/insurance panel open over the
top of the screen. Pre-existing state for that account, not a capture fault, but the shot is
partly obscured. Worth dismissing that panel before the next run if the catalog needs a clean
contractor view.

The Clear-filters button is correctly ABSENT from `admin/jobs`: it only renders when a filter is
active, and the capture starts with none.
