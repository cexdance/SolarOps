# 2026-06-02 Deployment

## Sidebar nav reorder — commit 2e065e2

**What changed:** Moved Customers (plus its sub-items Lead Lobby and SolarEdge Sites) from position 5 to position 3 in the left sidebar, directly after Dashboard.

**Why:** User request for faster access to Customers from top of nav.

**Files modified:**
- `solarflow-dashboard/src/components/Layout.tsx` — reordered `allNavItems` array

**Pattern:** Nav order is controlled by the `allNavItems` array in `Layout.tsx:59`. Sub-items stay adjacent to their parent and are identified by `indent: true, parent: 'customers'` etc. Moving a group means moving the parent row and all its indented children together.

**Also merged in this push (from debug/2026-06-02-0955-photo-idb-offload):**
- fix(photos): IDB-first capture, strip base64 from localStorage
- fix(photos): stop base64 mirroring into solarflow_data
- fix(photos): direct upload for instant admin sync + IDB durability
- fix(photos): replace fetch(dataUrl) with atob for iOS Safari

**Pending follow-ups:** None.
